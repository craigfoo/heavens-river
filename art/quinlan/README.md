# Quinlan source art

Source files for the Quinlan model: the Meshy export (GLB or FBX) and concept images. Nothing here ships with the site. The build only bundles what the code imports, and the game will load an optimised copy from `public/models/`.

If a model's licence asks for credit (for example CC BY), note it here and in the main README.
